# Optional observability stack for StockPlanBackend production/staging.
#
# Usage:
#   docker compose -f docker-compose.production.yml -f docker-compose.observability.yml up -d

volumes:
  grafana_data:

services:
  otel-collector:
    image: otel/opentelemetry-collector-contrib:0.111.0
    restart: unless-stopped
    command: ["--config=/etc/otelcol-contrib/config.yaml"]
    volumes:
      - ./monitoring/otel-collector.yaml:/etc/otelcol-contrib/config.yaml:ro
    networks:
      - internal
    depends_on:
      - jaeger

  jaeger:
    image: jaegertracing/all-in-one:1.62.0
    restart: unless-stopped
    environment:
      COLLECTOR_OTLP_ENABLED: "true"
    networks:
      - internal

  grafana:
    image: grafana/grafana:11.2.2
    restart: unless-stopped
    environment:
      GF_SECURITY_ADMIN_USER: ${GRAFANA_ADMIN_USER:-admin}
      GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_ADMIN_PASSWORD:?GRAFANA_ADMIN_PASSWORD is required}
      GF_USERS_ALLOW_SIGN_UP: "false"
      GF_SMTP_ENABLED: ${GRAFANA_SMTP_ENABLED:-true}
      GF_SMTP_HOST: ${GRAFANA_SMTP_HOST:?GRAFANA_SMTP_HOST is required}
      GF_SMTP_USER: ${GRAFANA_SMTP_USER:?GRAFANA_SMTP_USER is required}
      GF_SMTP_PASSWORD: ${GRAFANA_SMTP_PASSWORD:?GRAFANA_SMTP_PASSWORD is required}
      GF_SMTP_FROM_ADDRESS: ${GRAFANA_ALERT_EMAIL_FROM:?GRAFANA_ALERT_EMAIL_FROM is required}
      GRAFANA_ALERT_EMAIL_TO: ${GRAFANA_ALERT_EMAIL_TO:?GRAFANA_ALERT_EMAIL_TO is required}
      GRAFANA_SLACK_WEBHOOK_URL: ${GRAFANA_SLACK_WEBHOOK_URL:?GRAFANA_SLACK_WEBHOOK_URL is required}
    volumes:
      - grafana_data:/var/lib/grafana
      - ./monitoring/grafana/provisioning:/etc/grafana/provisioning:ro
    ports:
      - "127.0.0.1:3000:3000"
    networks:
      - internal
      - external
    depends_on:
      - jaeger
