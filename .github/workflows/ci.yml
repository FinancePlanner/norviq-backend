name: CI

on:
  # main is deliberately NOT excluded here.
  #
  # It used to be, on the assumption that the PR run covers whatever reaches
  # main. That assumption broke: a PR merged while its branch had newer fixes
  # that were not in the PR head, and main sat not compiling
  # (NewsSyncJob.swift calling a method that does not exist) with nothing to
  # catch it — the next deploy would have failed at image build.
  #
  # Feature branches already run twice (push + pull_request), so dropping the
  # exclusion changes nothing for them and closes the gap on the one branch
  # that actually ships.
  push:
    paths:
      - 'Sources/**'
      - 'Tests/**'
      - 'Package.swift'
      - 'Package.resolved'
  pull_request:
    branches:
      - main
    paths:
      - 'Sources/**'
      - 'Tests/**'
      - 'Package.swift'
      - 'Package.resolved'

jobs:
  lint:
    name: SwiftFormat + SwiftLint
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: SwiftFormat
        run: swiftformat --lint . --reporter github-actions-log
      - name: Install SwiftLint
        run: |
          set -euo pipefail
          if ! command -v swiftlint &> /dev/null; then
            curl -fsSL -o /tmp/swiftlint.zip \
              "https://github.com/realm/SwiftLint/releases/download/0.65.1/portable_swiftlint.zip"
            unzip -o /tmp/swiftlint.zip -d /tmp/swiftlint
            echo "/tmp/swiftlint" >> $GITHUB_PATH
          fi
      - name: SwiftLint
        run: swiftlint lint --reporter github-actions-logging

  test:
    name: Run Tests
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:18-alpine
        env:
          POSTGRES_USER: stockplan_user
          POSTGRES_PASSWORD: stockplan_password
          POSTGRES_DB: stockplan_test
        options: >-
          --health-cmd "pg_isready -U stockplan_user -d stockplan_test"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 5

      redis:
        image: redis:7-alpine
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 5

    container:
      image: swift:6.3.1-noble

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Cache Swift build
        uses: actions/cache@v4
        with:
          path: .build
          key: ${{ runner.os }}-swift-test-${{ hashFiles('Package.resolved') }}
          restore-keys: |
            ${{ runner.os }}-swift-test-

      - name: Run tests
        env:
          DATABASE_HOST: postgres
          DATABASE_PORT: 5432
          DATABASE_NAME: stockplan_test
          DATABASE_USERNAME: stockplan_user
          DATABASE_PASSWORD: stockplan_password
          REDIS_URL: redis://redis:6379
          LOG_LEVEL: error
        run: swift test
